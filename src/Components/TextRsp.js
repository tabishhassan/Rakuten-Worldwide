import React, { Component } from "react";
import {
  Button,
  Image,
  StyleSheet,
  Text,
  View,
  ScrollView,
  Linking,
  WebBrowser
} from "react-native";
import { NativeRouter, Route, Link } from "react-router-native";

class TextRsp extends Component {
  render() {
    return (
      <View>
        <Text>Ici le texte</Text>
      </View>
    );
  }
}

const styles = StyleSheet.create({});

export default TextRsp;
